# This returns the next random floating point number in the range (0.0, 1.0)
import random
print("Printing random number")
print(random.random())

#To generate random integers, we can use : random() and randrange()
import random
print("Printing random integers", random.randrange(0, 10))
